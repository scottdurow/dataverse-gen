//import { SchemaGenerator } from "../SchemaGenerator";

//import { TypescriptGenerator } from "../TypescriptGenerator";

describe("TypeScriptGenerator", () => {
  it("handles newlines in attribute description", () => {
    //const codeGenerator = new TypescriptGenerator(packageDir, projectDir, config);
  });

  it.todo("does not output Target on WhoAmI request");

  it.todo("sorts enums numerically");

  it.todo("filters based on publisher prefix");
});
